[project]
name = "rag-demo"
version = "0.1.0"
description = "Industrial PDF retrieval demo with Docling blocks and hybrid search"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "chromadb",
    "dashscope",
    "docling",
    "langchain-community",
    "llama-index-core",
    "numpy",
    "openai",
    "pymupdf",
    "python-dotenv",
    "rank-bm25",
    "sentence-transformers",
    "tqdm",
]
