# Industrial PDF RAG Demo 技术实现说明

## 1. 项目定位

本项目并不是一个普通的“PDF Chat”系统。

它的核心目标是：

```text
把工业维修手册 PDF
转化为：
可检索、可推理、可追溯的知识系统。
```

项目当前主要面向：

* 工业维修手册
* CNC / PLC 文档
* 报警说明书
* 参数说明书
* 故障排查文档
* 工业设备操作手册

工业 PDF 与普通 PDF 最大区别：

```text
1. layout 复杂
2. 大量表格
3. 报警码
4. 参数号
5. OCR 扫描页
6. hierarchy 混乱
7. 图文混排
8. warning / caution / danger
9. 大量非自然语言结构
```

因此：

```text
不能：
PDF -> chunk -> embedding
```

因为普通 chunk 无法理解：

* 哪个是报警
* 哪个是步骤
* 哪个是 warning
* 哪个是 table row
* 哪个是参数说明
* 哪个是安全提示

所以本项目采用：

```text
Layout-aware + Quality Gate + KB Normalize + Evidence-oriented RAG
```

架构。

---

# 2. 整体 Pipeline

完整链路：

```text
PDF
-> Docling layout / page images / layout.json
-> draft_blocks / final_blocks
-> garbled page detect
-> DashScope OCR fix
-> final_blocks_ocr_fixed
-> Knowledge Blocks
-> Knowledge Graph
-> Query Parse
-> Retrieval
-> Merge
-> Rerank
-> TopK Evidence Package
-> Rule-based Reasoning Planner
-> Evidence-grounded Prompt
-> LLM Answer
-> Validate
-> Final Answer
```

其中：

* 前半部分：离线知识构建
* 后半部分：在线检索与推理

---

# 3. 第一阶段：Layout Parsing

## PDF -> Docling layout / page images / layout.json

执行：

```bash
uv run python scripts/01_parse_pdf_light.py "data/raw/300220、FANUC0iMF维修说明书.pdf"
```

生成：

```text
data/parsed/300220_FANUC0iMF/layout.json
data/images/300220_FANUC0iMF/page_0001.png
data/images/300220_FANUC0iMF/page_0002.png
...
```

---

## 为什么第一步不是直接抽文本？

因为工业 PDF 不是连续自然语言文档。

它包含：

```text
- 报警表
- 参数表
- hierarchy
- 图片说明
- 操作步骤
- 安全警告
- 多栏 layout
- OCR 扫描页
```

如果直接抽纯文本：

```text
用户问：SP9137
系统可能找到文字
但不知道：
- 来自哪一页
- 属于哪个表格
- 是否是 warning
- 是否是 alarm define
```

因此必须保留：

```text
1. 页面结构
2. bbox
3. reading order
4. table region
5. image region
6. page image
```

这是：

```text
layout-aware parsing
```

思想。

---

## layout.json 的作用

layout.json 保存：

```text
- text region
- table region
- image region
- bbox
- reading order
- font info
- hierarchy
```

page images 保存：

```text
1. 原始页图
2. OCR 修复
3. 人工复核
4. 最终答案页图引用
```

后面的：

```text
OCR repair
Table parsing
Block merge
Hierarchy rebuild
```

都依赖 layout 信息。

---

# 4. 第二阶段：Block Building

## layout.json -> draft_blocks / final_blocks

执行：

```bash
uv run python scripts/02_build_blocks.py 300220_FANUC0iMF
```

生成：

```text
data/blocks/300220_FANUC0iMF/draft_blocks.jsonl
data/blocks/300220_FANUC0iMF/final_blocks.jsonl
```

---

## 为什么需要 blocks？

layout.json 仍然太底层。

RAG 系统真正需要的是：

```text
“可检索、可引用、可追溯”的知识片段
```

即：

```text
block
```

block 本质上是：

```text
结构化 chunk
```

而不是普通 chunk。

---

## draft_blocks vs final_blocks

### draft_blocks

第一轮粗切块结果。

特点：

```text
- 覆盖率高
- 速度快
- 可能存在误判
```

---

### final_blocks

第一轮 normalize 后的 blocks。

它会补充：

```text
1. block_id
2. hierarchy
3. searchable_text
4. context_prev/context_next
5. metadata normalize
6. block_type
7. page linkage
```

注意：

```text
final_blocks
并不是最终可信结果。
```

它只是：

```text
Docling light parse 阶段的标准化结果。
```

后面仍然可能：

```text
- OCR 修复
- refine parsing
- block merge
- KB normalize
```

---

# 5. Block 数据结构

一个 block 大概长这样：

```json
{
  // 当前 block 的唯一编号。
  // 300220_FANUC0iMF 表示文档 ID。
  // b000147 表示这是当前文档中的第 147 个 block。
  // 后面检索、知识图谱、引用、上下文关联都依赖这个 ID。
  "block_id": "300220_FANUC0iMF_b000147",

  // 文档 ID。
  // 对应 data/raw/300220、FANUC0iMF维修说明书.pdf
  // 用于区分不同 PDF 来源。
  "doc_id": "300220_FANUC0iMF",

  // 当前 block 类型。
  // 这里被识别成 text，而不是 warning。
  // 因为当前 light parse 阶段：
  // - Docling 解析出来的正文只有 "引导系统"
  // - "请勿湿者手进行作业" 被识别进了 title / hierarchy
  // - 正文区没有明显 WARNING/警告 关键词
  // - layout 特征也不明显
  // 所以规则系统暂时把它归类为普通 text。
  // 
  // 这正好体现：工业 PDF 的轻量解析阶段并不完美，
  // 后面 refined parsing 会进一步修正。
  // 
  // block_type 判断通常结合：
  // - layout 结构 | bbox 坐标 | 字体大小 | 粗体
  // - OCR 特征 | 正则规则 | 关键词 | 表格区域
  "block_type": "text",

  // block 层级。
  // 值越小通常越接近：章节标题、一级标题
  // 值越大越接近：正文、子内容、普通说明
  // 这里是 3：表示这是一个普通正文级 block。
  "block_level": 3,

  // 依据来源可信度优先级。
  // 后面如果 OCR 修复结果、refined parsing、人工修正、layout parser
  // 同时存在多个版本，系统会优先采用 source_priority 更高的版本。
  // 这里是 0.5：表示它只是 light parse 阶段的普通 layout 结果，可信度一般。
  //
  // 当前 Demo 里的 source_priority 主要是"来源静态基准分"。
  // 不同解析来源，系统默认赋予不同可信度：
  // - docling_layout：0.5
  // - OCR 修复：0.9
  // - refined parser：0.95
  // - manual fix：1.0
  //
  // 所以这个 block 之所以是 0.5，并不是算法实时算出来的，
  // 而是因为它来自 docling_layout。
  //
  // Docling light parse 虽然速度快、覆盖率高，但容易出现：
  // - hierarchy 错误 | warning/text 混淆 | table 断裂 | title/body 不一致
  // 所以默认不会给特别高的可信度。
  //
  // 后续生产级版本，可以进一步扩展成动态质量评分系统，
  // 结合 OCR confidence、文本质量、结构一致性、语义完整度等指标实时计算。
  "source_priority": 0.5,

  // 当前 block 来源于 PDF 第 7 页。
  "page_no": 7,

  // 当前 block 的标题。
  // 这里其实能看出来：layout parser 认为
  // "10 请勿湿者手进行作业..." 更像一个标题或章节。
  // 但实际上它更像安全警告语句。
  // 这说明：工业 PDF 的 layout 结构很复杂，
  // light parse 阶段可能出现标题误判、章节归属错误、warning/text 混淆。
  // 后面的 refine parser 会修正。
  "title": "10 请勿湿者手进行作业。否则恐会导致触电或损坏电路。",

  // Docling 原始解析文本。
  // 这里非常典型：页面真实语义是"请勿湿手作业，否则会触电"
  // 但正文只解析出了"引导系统"
  // 说明当前 block 存在语义缺失。
  // 这也是为什么工业 PDF 不能直接 chunk 后向量化。
  "raw_text": "引导系统",

  // 标准化后的文本。
  // canonical_text 通常会：去掉异常空格、修复换行、清洗乱码、标准化符号
  // 当前因为 raw_text 很短，所以 canonical_text 与 raw_text 相同。
  "canonical_text": "引导系统",

  // 检索文本。专门给 BM25、向量检索、混合召回使用的文本。
  // searchable_text 并不一定等于正文，它通常会融合：
  // title、canonical_text、section_path、alarm_code、hierarchy、同义词扩展
  //
  // 这里系统故意把"请勿湿者手进行作业"拼接进 searchable_text。
  // 因为：虽然正文解析失败，但 title/hierarchy 里存在重要语义。
  // 如果不拼进去，用户搜索"触电"、"湿手"、"安全警告"就完全召回不到。
  "searchable_text": "10 请勿湿者手进行作业。否则恐会导致触电或损坏电路。
10 请勿湿者手进行作业。否则恐会导致触电或损坏电路。
引导系统",

  // 当前 block 中识别到的报警码。
  // 这里为空：表示没有识别出 SP9137、ALM-101、ERR-xx 这类报警码。
  "alarm_codes": [],

  // 当前 block 所属章节路径。
  // 这里再次体现：layout parser 把安全警告误当成了章节路径。
  // 这类错误在工业 PDF 很常见，所以后面需要 refined parsing + KB normalize。
  "section_path": "10 请勿湿者手进行作业。否则恐会导致触电或损坏电路。",

  "hierarchy": {
    // 当前 chapter。同样存在"安全警告被误识别为章节标题"的问题。
    "chapter": "10 请勿湿者手进行作业。否则恐会导致触电或损坏电路。",
    "section": "",   // section 为空
    "subsection": "" // subsection 为空
  },

  // 前一个 block，用于保持阅读顺序。
  "prev_block_id": "300220_FANUC0iMF_b000146",

  // 后一个 block，用于上下文关联。
  "next_block_id": "300220_FANUC0iMF_b000148",

  // 父级 block ID。
  // 如果当前 block 属于某个章节、某个表格、某个报警组，会指向父级。
  // 当前为空。
  "parent_block_id": null,

  // 前文上下文 block。
  // 检索时可以把附近 block 一起带回来，避免只命中孤立短句。
  "context_prev": [
    "300220_FANUC0iMF_b000145",
    "300220_FANUC0iMF_b000146"
  ],

  // 后文上下文 block。
  // 用于 context expansion、merge retrieval、evidence package。
  "context_next": [
    "300220_FANUC0iMF_b000148",
    "300220_FANUC0iMF_b000149"
  ],

  // 当前页上下文 ID。
  // 表示这个 block 属于第 7 页整体上下文。
  "page_context_id": "300220_FANUC0iMF_page_0007_context",

  // 对应 layout.json 中的引用位置。
  // $.texts[146] 表示这个 block 来源于 layout.json 的 texts 数组第 146 项。
  // 后面 OCR 修复、bbox 回查、页面定位都会用到。
  "layout_refs": ["$.texts[146]"],

  // 当前 block 在 PDF 页面中的坐标。
  // 格式：[x1, y1, x2, y2]
  // 用于页面定位、表格分析、OCR 区域裁剪、高亮引用。
  "bbox": [
    87.81101500000014,
    364.28030000000047,
    140.79401500000014,
    355.1180272727278
  ],

  // 当前 block 对应的页图路径。
  // 后面 OCR、人工复核、UI 引用页截图都依赖这个路径。
  "image_paths": ["data/images/300220_FANUC0iMF/page_0007.png"],

  // 表示当前 block 有对应页图。
  "has_image": true,

  // OCR 置信度。
  // 当前为空：表示这个 block 不是 OCR 生成，而是 Docling layout 解析结果。
  "ocr_confidence": null,

  // 当前 block 来源。
  // 这里来自 Docling layout parser。
  // 后面可能还存在 dashscope_ocr、refined_parser、manual_fix。
  "source": "docling_layout",

  // 是否需要进一步精解析。
  // 当前是 false。
  // 但实际上从语义角度看，这个 block 非常值得 refine。
  // 因为：warning/text 混淆、hierarchy 误判、正文缺失。
  //
  // 当前 block 被标记为 text，needs_refine=false，
  // 但从 title、section_path 和 searchable_text 看，它明显包含安全警告语义。
  //
  // 所以生产环境不能只依赖初次 parser，而要增加 Block Quality Gate。
  // 质量门会检查字段冲突、短正文长标题、安全关键词、hierarchy 异常、title/body 不一致等。
  // 一旦命中，就把 block 标记为 needs_refine=true，进入二次精解析。
  //
  // Demo 版本先跑通链路；生产版本要补质量检测、精解析回写和低置信度证据降级机制。
  "needs_refine": false,

  // refine 原因。
  // 如果 alarm dense、table dense、OCR low confidence、layout conflict，会记录原因。
  "refine_reason": [],

  // 当前解析级别。
  // light：轻量快速解析。后面可能还有 refined、deep_parse。
  "parse_level": "light",

  // 创建时间
  "created_at": "2026-05-14T02:25:14Z",

  // 更新时间
  "updated_at": "2026-05-14T02:25:14Z"
}
```

# 5.1 Block 是怎么生成的？

Block 生成这块是整个离线解析的重点。

很多人容易误解为：

```text
PDF 从第一页开始扫描
-> 每隔固定长度切一块
-> 得到 blocks
```

但这里的 block 并不是固定大小 chunk。

它更接近：

```text
基于 layout 的语义切块
```

也就是说：

```text
先按页面解析 layout
再根据页面元素、标题、表格、步骤、报警码、bbox、reading order
组合成一个个可检索、可引用、可追踪的 block。
```

---

## 5.1.1 为什么看起来像“从第一页开始”？

因为 PDF 本身是分页文档。

Docling 解析时天然会得到：

```text
page_1
page_2
page_3
...
```

所以 block 构建一般会按：

```text
page_no asc
-> reading_order asc
```

顺序扫描。

这一步的目的不是按页切死，而是为了：

```text
1. 保持原文阅读顺序
2. 生成 prev_block_id / next_block_id
3. 建立 page_context_id
4. 保留 bbox 和 page image 引用
5. 后续可以回到原文页图复核
```

所以：

```text
按页面顺序处理 ≠ 按页面强行切块
```

---

## 5.1.2 Block 和固定 chunk 的区别

普通 RAG chunk 通常是：

```text
每 500 tokens 切一块
overlap 50 tokens
```

这种方式的问题是：

```text
1. 不知道这是标题还是正文
2. 不知道这是 warning 还是普通说明
3. 不知道这是表格行还是段落
4. 不知道来自哪一个 bbox
5. 不知道上下文关系
6. 不知道是否需要 OCR 修复
```

而本项目的 block 是：

```text
layout-aware structured chunk
```

它不仅有文本，还有：

```text
- page_no
- bbox
- block_type
- hierarchy
- section_path
- layout_refs
- image_paths
- prev/next
- context_prev/context_next
- source_priority
- needs_refine
```

所以 block 不是简单切文本，而是：

```text
带结构、位置、来源、上下文、质量信息的工业文档知识片段。
```

---

## 5.1.3 Block 生成的大致逻辑

可以理解为：

```text
layout.json
-> 遍历 page
-> 读取 text/table/image elements
-> 按 reading order 排序
-> 合并相邻碎片
-> 识别标题 / 正文 / 表格 / warning / alarm
-> 生成 block
-> 补充上下文关系
-> 输出 draft_blocks
-> normalize 后输出 final_blocks
```

伪代码：

```python
for page in document.pages:
    elements = sort_by_reading_order(page.elements)

    current_section = update_section_context(elements)

    for element in elements:
        block = build_block(element)
        block.page_no = page.page_no
        block.bbox = element.bbox
        block.layout_refs = element.layout_ref
        block.section_path = current_section.path
        block.block_type = infer_block_type(element, current_section)
        block.searchable_text = build_searchable_text(block)
        blocks.append(block)

link_prev_next(blocks)
build_context_window(blocks)
write_draft_blocks(blocks)
write_final_blocks(normalize(blocks))
```

---

## 5.1.4 那跨页内容怎么办？

这是重点。

工业手册里确实会出现：

```text
第一页出现安全说明
第二页继续解释
第 100 页又引用同一个报警码或部件
```

所以 block 构建阶段不会试图一次性解决所有跨页语义关系。

它分三层处理：

```text
第一层：顺序上下文
第二层：章节上下文
第三层：知识图谱 / 检索扩展
```

---

### 第一层：顺序上下文

每个 block 会保留：

```json
"prev_block_id": "...",
"next_block_id": "...",
"context_prev": ["...", "..."],
"context_next": ["...", "..."]
```

作用是：

```text
即使 block 本身很短，检索时也能把前后内容一起带回来。
```

例如当前 block 只有：

```text
引导系统
```

但它前后 block 可能包含：

```text
请勿湿手作业
否则可能导致触电
安全注意事项
```

通过 context_prev/context_next，系统可以补足局部上下文。

---

### 第二层：章节上下文

每个 block 会保留：

```json
"section_path": "...",
"hierarchy": {
  "chapter": "...",
  "section": "...",
  "subsection": "..."
}
```

作用是：

```text
把分散在不同页面但属于同一章节的内容归到一起。
```

例如：

```text
安全注意事项
  -> 第 6 页
  -> 第 7 页
  -> 第 8 页
```

即使内容跨页，也能通过 hierarchy 归属到同一个 section。

---

### 第三层：Knowledge Graph / Retrieval Expansion

有些关系不是连续页面关系，而是远距离语义关系。

例如：

```text
第 7 页：请勿湿手作业，否则可能触电
第 100 页：电气柜维护步骤
第 300 页：主轴放大器报警
第 620 页：SP9137 通信异常
```

这些内容不一定相邻，也不一定在同一章节。

这时靠：

```text
Knowledge Blocks
Knowledge Graph
Query Parse
Retrieval Expansion
```

来关联。

比如图谱里可以建立：

```text
SP9137
-> spindle amplifier
-> electrical inspection
-> power off warning
-> communication cable
```

所以跨页、远距离关系不是在 block 生成阶段全部解决，而是在后续 KB / Graph / Retrieval 阶段解决。

---

## 5.1.5 为什么不在 block 阶段就把所有相关页合并？

因为会有几个问题：

```text
1. block 会变得太大，检索不精准
2. 远距离关系容易误合并
3. 一个报警可能关联多个章节，无法简单拼成一个块
4. 后续引用页码会变得不清晰
5. LLM 输入会变长，噪声变多
```

所以正确做法是：

```text
Block 阶段保持“小而可追踪”
KB 阶段做语义拆解
Graph 阶段做关系连接
Retrieval 阶段按 query 动态扩展上下文
```

---

## 5.1.6 可以这样理解

```text
Block 不是最终知识答案。
Block 是工业文档的结构化索引单元。
```

它主要负责：

```text
1. 保存原文位置
2. 保存局部语义
3. 保存检索文本
4. 保存上下文指针
5. 保存质量信息
```

真正解决跨页、跨章节、跨概念关联的是：

```text
Knowledge Blocks
Knowledge Graph
Retrieval Expansion
Evidence Package
```

所以：

```text
从第一页开始生成 block 是合理的。
因为这是在重建文档顺序。

但系统不是只依赖页面顺序。
它还会通过 hierarchy、context、graph、retrieval expansion
处理跨页和远距离关联。
```

# 6. 为什么这个 block 很重要？

这个 block 非常典型。

因为它暴露了：

```text
工业 PDF 的 light parse 并不可靠。
```

例如：

| 字段         | 问题            |
| ---------- | ------------- |
| title      | 明显是安全警告       |
| raw_text   | 只有“引导系统”      |
| hierarchy  | 被误识别成 chapter |
| block_type | 被识别成 text     |

这说明：

```text
title / body / hierarchy
可能互相冲突。
```

这也是为什么：

```text
工业 PDF 不能：
PDF -> chunk -> embedding
```

---

# 7. block_type 是怎么判断的？

block_type 不是 LLM 判的。

而是：

```text
layout + regex + rules + heuristics
```

例如：

```python
if warning_keyword:
    block_type = warning

elif alarm_regex:
    block_type = alarm_candidate

elif table_region:
    block_type = table_candidate
```

block_type 判断通常结合：

```text
- layout 结构
- bbox 坐标
- 字体大小
- 粗体
- OCR 特征
- 正则规则
- 关键词
- 表格区域
```

常见 block_type：

| type            | 含义   |
| --------------- | ---- |
| text            | 普通正文 |
| warning         | 安全警告 |
| alarm_candidate | 报警候选 |
| table_candidate | 表格候选 |
| procedure       | 操作步骤 |
| parameter       | 参数说明 |
| image_caption   | 图片说明 |

---

# 8. source_priority 是什么？

source_priority 本质上是：

```text
当前 block 来源可信度。
```

因为工业 PDF 在解析过程中：

```text
同一段内容
可能有多个版本。
```

例如：

```text
1. docling_layout
2. OCR
3. refined parser
4. manual fix
```

系统需要知道：

```text
最终更相信谁。
```

当前 Demo 里的 source_priority：

```text
主要是“来源静态基准分”。
```

例如：

| source         | priority |
| -------------- | -------- |
| manual_fix     | 1.0      |
| refined_parser | 0.95     |
| dashscope_ocr  | 0.9      |
| docling_layout | 0.5      |

所以：

```json
"source": "docling_layout",
"source_priority": 0.5
```

并不是动态 AI 算法评分。

而是：

```text
当前来源默认可信度。
```

后续生产级可以扩展：

```text
priority =
base_source_score
+ OCR_confidence
+ semantic_integrity
+ structure_score
- anomaly_penalty
```

---

# 9. 为什么需要 needs_refine？

当前 block：

```json
"needs_refine": false
```

但实际上：

```text
它非常值得 refine。
```

因为：

```text
- warning/text 混淆
- hierarchy 错误
- 正文缺失
- title/body semantic mismatch
```

这说明：

```text
light parse 不可靠。
```

因此生产级必须增加：

```text
Block Quality Gate
```

即：

```text
字段一致性检查
```

例如：

```text
1. title/body mismatch
2. hierarchy conflict
3. short body + long title
4. warning keyword but type=text
5. OCR low confidence
```

一旦命中：

```json
{
  "needs_refine": true,
  "refine_reason": [
    "warning_keyword_but_not_warning_type",
    "title_body_mismatch"
  ]
}
```

然后进入：

```text
refined parsing
```

阶段。

---

# 10. 第三阶段：Garbled Page Detect

## final_blocks -> garbled detect

执行：

```bash
uv run python scripts/14_detect_garbled_pages.py data/blocks/300220_FANUC0iMF/final_blocks.jsonl
```

生成：

```text
data/ocr/300220_FANUC0iMF/garbled_pages.json
```

---

## 为什么需要 garbled detect？

因为：

```text
final_blocks 解决：
“有没有结构”

garbled detect 解决：
“文本质量是否可信”
```

工业 PDF 经常出现：

```text
□□□
���
乱码
OCR 错误
```

如果乱码直接进入向量库：

```text
后面 retrieval 一定会出问题。
```

因此：

```text
先 detect
再 repair
```

这是：

```text
质量控制层
```

而不是重复解析。

---

# 11. 第四阶段：DashScope OCR Fix

## garbled pages -> OCR

执行：

```bash
uv run python scripts/15_ocr_pages_dashscope.py 300220_FANUC0iMF
```

生成：

```text
data/ocr/300220_FANUC0iMF/ocr_pages.jsonl
```

---

## 为什么不全量 OCR？

因为：

```text
全量 OCR：
1. 成本高
2. 速度慢
3. layout 容易丢
4. 正常页可能被 OCR 污染
```

因此采用：

```text
先 Docling
后 Quality Gate
只 OCR 问题页
```

这是：

```text
incremental repair architecture
```

---

# 12. 第五阶段：final_blocks_ocr_fixed

执行：

```bash
uv run python scripts/16_fix_blocks_with_ocr.py ^
  data/blocks/300220_FANUC0iMF/final_blocks.jsonl ^
  data/ocr/300220_FANUC0iMF/ocr_pages.jsonl
```

生成：

```text
data/blocks/300220_FANUC0iMF/final_blocks_ocr_fixed.jsonl
```

---

## 为什么需要 merge back？

OCR 结果不能独立存在。

否则：

```text
retrieval
KB
graph
```

会出现：

```text
双数据源问题。
```

因此：

```text
OCR repair
-> merge back
-> final_blocks_ocr_fixed
```

统一后续 pipeline。

---

# 13. 第六阶段：Knowledge Blocks

## final_blocks -> KB blocks

执行：

```bash
uv run python scripts/12_build_kb_blocks.py data/blocks/300220_FANUC0iMF/final_blocks_ocr_fixed.jsonl
```

生成：

```text
data/kb/300220_FANUC0iMF/kb_blocks.jsonl
```

---

## 为什么还需要 KB Blocks？

因为：

```text
final_blocks：
偏“检索”

KB blocks：
偏“推理”
```

一个 alarm block 可能同时包含：

```text
- alarm define
- cause
- procedure
- warning
- note
```

如果直接给 LLM：

```text
LLM 很难稳定组织答案。
```

因此需要：

```text
semantic decomposition
```

拆成：

```text
- alarm（报警定义）
- cause（可能原因）
- procedure（检查步骤 / 处理流程）
- warning（安全警告）
```

最终系统才能稳定输出：

```text
- 报警结论
- 可能原因
- 检查步骤
- 安全提醒
- 手册出处
- 需要补充的信息
```

---

# 14. 第七阶段：Knowledge Graph

执行：

```bash
uv run python scripts/13_build_kb_graph.py data/kb/300220_FANUC0iMF/kb_blocks.jsonl
```

生成：

```text
data/kb/300220_FANUC0iMF/kb_graph.json
```

---

## 为什么需要 Graph？

Graph 不是为了替代 embedding。

而是：

```text
relation expansion layer
```

即：

```text
上下文关系扩展。
```

例如：

```text
SP9137
-> spindle amplifier
-> communication cable
-> warning
-> same page alarm
```

不同检索方式：

| 方法        | 擅长                  |
| --------- | ------------------- |
| embedding | semantic similarity |
| BM25      | keyword match       |
| graph     | relation expansion  |

Graph 用于：

```text
补全上下文关系。
```

而不是替代向量检索。

---

# 15. 生产环境架构

当前 Demo：

```text
JSONL
```

原因：

```text
方便调试
方便复现
方便演示
```

---

## 生产级建议

### 对象存储

存：

```text
- PDF
- page image
- OCR crop image
- layout raw file
```

例如：

```text
MinIO / S3
```

---

### 关系数据库

存：

```text
- document
- page
- block
- kb_block
- parse_job
- version
- audit
```

例如：

```text
PostgreSQL / MySQL
```

---

### 检索引擎

BM25：

```text
Elasticsearch / OpenSearch
```

向量检索：

```text
Milvus / Qdrant / pgvector
```

Knowledge Graph：

```text
Neo4j / ArangoDB / PostgreSQL relation table
```

---

# 16. 项目真正验证的核心

这个项目真正验证的：

并不是：

```text
某个 embedding 模型
某个 parser
某个向量库
```

而是：

```text
工业 PDF
如何从“视觉文档”
变成：
可检索、可推理、可追溯的知识系统。
```

核心思想：

```text
Layout-aware
+ Quality Gate
+ Incremental OCR Repair
+ Knowledge Normalize
+ Evidence-oriented Retrieval
+ Structured Reasoning
```

而不是：

```text
PDF -> chunk -> embedding -> LLM
```


